// src/styles/theme.ts
import {DefaultTheme} from 'styled-components';

const theme: DefaultTheme = {
    basicWidth: '320px',

    color: {
        // main: "#1c1f25",
        main: "lightsteelblue",
        sub: "#fff"
    }
};

// export type Theme = typeof theme;

export {theme};